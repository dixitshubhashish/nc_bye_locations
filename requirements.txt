# Core dependencies for Location Intelligence Platform & Whitespace Analysis Tool
# Python 3.10+ required (uses PEP 604 unions like `str | None`).

# BigQuery warehouse client & Google authentication
google-cloud-bigquery>=3.0.0
google-auth>=2.0.0

# DataFrame & Parquet processing (staging/pushing unified warehouse tables)
pandas>=1.5.0
pyarrow>=10.0.0

# Legacy Excel (.xls) file parsing adapter
# Note: Modern OpenXML (.xlsx) files are parsed via stdlib (zipfile + xml.etree.ElementTree)
xlrd>=2.0.1

# Environment variable management (.env support)
python-dotenv>=1.0.0

# Optional HTTP requests library for external data source connectors
requests>=2.28.0
